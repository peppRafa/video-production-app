{
  "name": "video-production-app",
  "version": "1.0.0",
  "description": "Video Production Management App with AI Integration",
  "main": "server/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-deps": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "cd server && npm start",
    "heroku-postbuild": "cd server && npm install && cd ../client && npm install && npm run build"
  },
  "keywords": ["video-production", "project-management", "ai", "react", "nodejs"],
  "author": "Video Production Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
